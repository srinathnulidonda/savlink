{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "gunicorn run:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 60 --log-level info --access-logfile - --error-logfile -",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}